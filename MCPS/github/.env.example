# =============================================================================
# GitHub MCP Server - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values.
# =============================================================================

# -----------------------------------------------------------------------------
# GitHub Authentication (REQUIRED)
# -----------------------------------------------------------------------------
# Fine-grained Personal Access Token with repository permissions.
#
# To create a token:
# 1. Go to: GitHub -> Settings -> Developer settings -> Personal access tokens
#    -> Fine-grained tokens
# 2. Click "Generate new token"
# 3. Set token name (e.g., "claude-assistant-github-mcp")
# 4. Set expiration (recommended: 90 days)
# 5. Select repository access (specific or all)
# 6. Under "Repository permissions", grant:
#    - Contents: Read and write (for branches, file access)
#    - Issues: Read and write (for issue management)
#    - Pull requests: Read and write (for PR management)
#    - Metadata: Read-only (auto-selected)
# 7. Click "Generate token" and copy it here
#
GITHUB_TOKEN=github_pat_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# -----------------------------------------------------------------------------
# GitHub API Configuration (Optional)
# -----------------------------------------------------------------------------
# Base URL for GitHub API (change for GitHub Enterprise)
# GITHUB_API_BASE_URL=https://api.github.com

# Request timeout in seconds
# GITHUB_REQUEST_TIMEOUT=30

# Maximum retries for rate limit errors
# GITHUB_MAX_RETRIES=3

# -----------------------------------------------------------------------------
# Server Configuration (Optional)
# -----------------------------------------------------------------------------
# Host address to bind to
# GITHUB_MCP_HOST=0.0.0.0

# Port number to listen on
# GITHUB_MCP_PORT=8083

# Logging level (DEBUG, INFO, WARNING, ERROR)
# LOG_LEVEL=INFO
